export const queryAll = (el, parent = document) => parent.querySelectorAll(el);
export const query = (el, parent = document) => parent.querySelector(el);
export const queryId = (el, parent = document) => parent.getElementById(el);
